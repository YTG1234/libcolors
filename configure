#!/usr/bin/env bash

mkdir -p out
cd out

[[ ! -z $1 ]] && test=true

shift 1

if [ -z $test ]; then
    cmake .. -G "Unix Makefiles" -DCMAKE_BUILD_TYPE=release "$@"
else
    cmake .. -G "Unix Makefiles" -DCMAKE_BUILD_TYPE=release -DTEST_LIB=ON "$@"
fi
